'use strict';

/**
 * users-permissions-user service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::users-permissions-user.users-permissions-user');
