'use strict';

/**
 * users-permissions-user router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::users-permissions-user.users-permissions-user');
